<p>
    VITE_HEY:
    <span class="font-mono font-bold">{import.meta.env.VITE_HEY}</span>
</p>

<p>
    HEY_FASTIFY
    <span class="text-xs text-red-600">(you cannot access backend envs on the
        frontend)</span>:
    <span class="font-mono font-bold">{process.env.HEY_FASTIFY}</span>
</p>
